fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0

# 文件解析
PyMuPDF==1.25.2
python-docx==1.1.2

# OCR（扫描件 PDF）—— Qwen-VL-OCR 云端 API（阿里云 DashScope），通过 requests 调用
# 备用方案 RapidOCR 已在 ocr_engine.py 注释保留，需要时再装：
#   pip install rapidocr-onnxruntime onnxruntime
requests==2.32.3

# LLM 调用（OpenAI SDK 兼容 DeepSeek）
openai==1.61.0

# 代码质量
ruff==0.9.2
